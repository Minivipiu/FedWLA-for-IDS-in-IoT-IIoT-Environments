[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dt-fedwla"
version = "1.0.0"
description = "Federated Hyperparameter Optimisation (HPO) for Decision Tree using FedWLA strategy"
license = "MIT"
dependencies = [
    "flwr[simulation]>=1.19.0",
    "numpy",
    "pandas",
    "scikit-learn",
    "filelock"
]

[tool.flwr.app]
publisher = "minivipiu"

[tool.flwr.app.components]
serverapp = "dt_fedwla.server:app"
clientapp = "dt_fedwla.client:app"

[tool.flwr.app.config]
num-server-rounds = 10
dataset-path = "../data/EdgeIIoT_cleaned.csv"
imbalance-factor = 5
num-partitions = 10
